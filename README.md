# Proxy Config

Configurations for Clash, Surge, Shadowrocket, etc.

These configurations are only suitable for myself.

Update [rule providers](clash/rule_provider/) for clash, and then generate rule sets for other apps:

```bash
./generate.sh
```
